apiVersion: v1
kind: Service
metadata:
  name: nodejs-service
sepc:
  type: NodePort
  selector:
    app: nodejs-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080